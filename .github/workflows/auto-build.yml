name: Auto Build & Deploy

on:
  push:
    branches:
      - main
    paths:
      - 'openlayers/**'
      - 'cesium/**'
      - 'index.html'
      - 'static/**'

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            openlayers:
              - 'openlayers/**'
            cesium:
              - 'cesium/**'
            root:
              - 'index.html'
              - 'static/**'

      - name: Build openlayers
        if: steps.filter.outputs.openlayers == 'true'
        run: cd openlayers && npm ci && npm run build

      - name: Build cesium
        if: steps.filter.outputs.cesium == 'true'
        run: cd cesium && npm ci && npm run build

      - name: Prepare site
        run: |
          rm -rf _site
          mkdir -p _site
          cp index.html _site/
          cp -r static _site/
          [ -d "openlayers/dist" ] && cp -r openlayers/dist _site/openlayers/
          [ -d "cesium/dist" ] && cp -r cesium/dist _site/cesium/

      - name: Setup Pages
        uses: actions/configure-pages@v5
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          # Upload entire repository
          path: './_site'
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
